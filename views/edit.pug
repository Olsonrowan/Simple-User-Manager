html
    head
        tite **Please edit the form to edit account!**
    body
        form(action=`/edit/${editedUser.id}` method="post")
            div Name
            input(type='text', name="name", placeholder="Name", value=`${editedUser.name}`)
            div Email
            input(type='email', name="email", placeholder="Email", value=`${editedUser.email}`)
            div Age
            input(type="number" name='age', value=`${editedUser.age}`)
            button(type="Submit") Submit

            